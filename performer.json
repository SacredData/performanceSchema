{
  "type": "object",
  "title": "Performer",
  "required": [
    "lastName",
    "instrument"
  ],
  "properties": {
    "firstName": {
      "type": "string",
      "example": "Andrew"
    },
    "lastName": {
      "type", "string",
      "example": "Grathwohl"
    },
    "instrument": {
      "type": "object",
      "properties": {
        "device": {
          "type": "string",
          "example": "violin"
        }
      }
    }
  }
}
