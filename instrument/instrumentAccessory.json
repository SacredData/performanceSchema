{
  "type": "object",
  "title": "Instrument Accessory",
  "required": [
    "device"
  ],
  "properties": {
    "make": {
      "type": "string"
    },
    "model": {
      "type": "string"
    },
    "device": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": ["string", "reed", "bow", "rosin", "pickup", "amplifier", "foot pedal", "mute", "slide", "found object", "other"]
      }
    },
    "tuning": {
      "type": "string"
    }
  }
}
